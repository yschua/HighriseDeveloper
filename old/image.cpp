//import sys, os
//from PySFML import sf
#include <vector>
#include "SFML/Graphics.hpp"
#include "Background.h"
#include "Image.h"

CImage::CImage()
{
}

sf::Sprite* CImage::Load( string name )
{
   //if( !m_ImageManImages.has( name )
   //{ //if not name in image_man_images :
   //   m_ImageManImages[name] = sf.Image ()
   //   m_ImageManImages[name].LoadFromFile (os.path.join ("data", name))
   //}
   //return new sf::Sprite( image_man_images[name] );
   return 0;  //TODO
}